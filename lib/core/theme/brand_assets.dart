/// Canonical paths to bundled brand assets. Change here, used everywhere.
class BrandAssets {
  BrandAssets._();

  /// Full logo with the IGOBI wordmark — use on splash, marketing, large surfaces.
  static const String logoFull = 'assets/brand/logo.jpeg';

  /// Logo mark only (square, no wordmark) — use anywhere a small avatar-sized
  /// brand tile fits (sign-in hero tile, app bar logo, favicon).
  static const String logoMark = 'assets/brand/logo-mark.jpeg';

  /// Animated brand reveal for splash. mp4, ~1.4 MB. Plays muted + looping.
  static const String logoVideo = 'assets/brand/logo-vid.mp4';
}
